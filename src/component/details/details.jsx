import { useContext } from "react";
import {menu} from "../dashboard/dashboard";

export function Details(){
    const data= useContext(menu);
    
    return(
        <>
              hii
        </>
    )
}